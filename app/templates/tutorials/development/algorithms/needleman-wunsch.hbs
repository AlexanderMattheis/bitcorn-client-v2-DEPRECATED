<div class="content">
  <h2>
    Needleman-Wunsch
    <a href="https://doi.org/10.1016/0022-2836(70)90057-4">1970</a>
  </h2>
  <h4>by Needleman B. Saul, Wunsch D. Christian</h4>

  <h3>Description</h3>
  <div id="algorithm-description">
    <p>
      Computes an optimal, global alignment for two strings $a$ and $b$ using a linear scoring-function $s$.
      In this connection, the matrix $D$ represents a dynamic programming matrix which is computed by applying
      the named scoring-function $s$ from the top left corner.
      The shown solutions are then retrieved by applying the scoring-function backwards
      from the bottom right corner i.e. negate the mathematical sign (+/-).
      Programming-technically therefore, a recursive depth-first search can be applied that stops
      when the position $(0,0)$ (top-left) is reached.
    </p>
  </div>

  <br>

  <iframe width="540" height="304"
          frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>

  <br>

  <h3>Input</h3>
  <div id="algorithm-input">
    <p>
      <label class="input" for="sequence-a">
        <span>Sequence $a$:</span>
      </label>

      <input type="text" id="sequence-a" placeholder='e.g. "AACGT"'>
    </p>

    <p>
      <label class="input" for="sequence-b">
        <span>Sequence $b$:</span>
      </label>

      <input type="text" id="sequence-b" placeholder='e.g. "ACGT"'>
    </p>

    <div class="function">
      <label class="input">
        <span>Scoring $s$:</span>
      </label>

      <div>
        <label for="gap">
          <span>Gap</span>
        </label>

        <input type="number" class="number" id="gap">
      </div>

      <div>
        <label for="gap">
          <span>Match</span>
        </label>

        <input type="number" class="number" id="match">
      </div>

      <div>
        <label for="mismatch">
          <span>Mismatch</span>
        </label>

        <input type="number" class="number" id="mismatch">
      </div>
    </div>
  </div>

  <br>

  <h3>Output</h3>
  <div id="algorithm-output">
    <div class="doubled-columned">
      <div class="left-part">
        {{dynamic-matrix matrix-name="D"
                         sequenceA=(test-sequence "AACGT")
                         sequenceB=(test-sequence "ACGT")
                         matrix=(test-matrix 6 5)
                         score=5}}
      </div>

      <div class="right-part">
        {{results-table results=(test-results)}}
      </div>
    </div>

    <div class="single-columned">
      <a href="#">Download</a>
    </div>
  </div>
</div>